# Documentation for `eval.py`

## Overview
==========================================================
 Script: eval.py
 Author: M. El Aabaribaoune (@um6p)
 Description:
     Main evaluation entry point.

     - Loads configuration
     - Loads test data
     - Preprocesses inputs
     - Runs model evaluation and saves outputs
==========================================================

## Functions
### `def main(...)`
No description.

## How to Modify
If you need to make changes to `eval.py`:
- **Logic changes**: Locate the corresponding function. Update its docstring if you change its signature or behavior.
- Ensure any related imports in other files are updated if you change function/class names.